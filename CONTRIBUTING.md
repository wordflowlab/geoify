# 贡献指南

感谢你对 Geoify 的关注!本指南将帮助你了解如何为项目做出贡献。

## 目录

- [行为准则](#行为准则)
- [如何贡献](#如何贡献)
- [开发环境设置](#开发环境设置)
- [代码规范](#代码规范)
- [提交指南](#提交指南)
- [测试要求](#测试要求)

## 行为准则

### 我们的承诺

为了营造开放友好的环境,我们承诺:
- 尊重不同观点和经验
- 优雅地接受建设性批评
- 关注对社区最有利的事情
- 对其他社区成员表示同理心

### 不可接受的行为

- 使用性暗示语言或图像
- 人身攻击或侮辱性评论
- 公开或私下骚扰
- 未经明确许可发布他人信息

## 如何贡献

### 报告 Bug

提交 Issue 前,请先搜索是否已有类似问题。如果没有,请创建新 Issue 并提供:

1. **清晰的标题**
2. **详细描述**:
   - 问题是什么?
   - 期望的行为?
   - 实际的行为?
3. **复现步骤**
4. **环境信息**:
   - Node.js 版本
   - Geoify 版本
   - 操作系统
5. **相关代码或截图**

**Issue 模板**:
```markdown
## Bug 描述
简要描述问题

## 复现步骤
1. 运行命令 `geoify review article.md`
2. 看到错误...

## 期望行为
应该...

## 实际行为
但是...

## 环境
- Geoify 版本: 0.1.0
- Node.js 版本: 18.0.0
- 操作系统: macOS 14.0
```

### 建议新功能

我们欢迎新功能建议!请创建 Issue 并说明:

1. **功能描述**: 这个功能是什么?
2. **使用场景**: 为什么需要这个功能?
3. **建议实现**: 如何实现?(可选)
4. **替代方案**: 是否考虑过其他方案?

### 提交 Pull Request

1. **Fork 仓库**
2. **创建分支**: `git checkout -b feature/amazing-feature`
3. **开发并测试**
4. **提交代码**: 遵循提交规范
5. **推送分支**: `git push origin feature/amazing-feature`
6. **创建 Pull Request**

## 开发环境设置

### 前置要求

- Node.js >= 18.0.0
- npm >= 9.0.0
- Git

### 克隆仓库

```bash
git clone https://github.com/wordflowlab/geoify.git
cd geoify
```

### 安装依赖

```bash
npm install
```

### 开发命令

```bash
# 开发模式(使用 tsx)
npm run dev -- review test-article.md

# 构建
npm run build

# 运行测试
npm test

# 测试 UI
npm run test:ui

# 生成覆盖率
npm run test:coverage

# 清理构建
npm run clean
```

### 项目结构

```
geoify/
├── src/                    # 源代码
│   ├── cli.ts             # CLI 入口
│   ├── commands/          # CLI 命令
│   ├── scoring/           # 评分系统
│   ├── schema/            # Schema 生成器
│   └── reports/           # 报告生成器
├── test/                  # 测试文件
│   ├── scoring/          # 评分测试
│   └── schema/           # Schema 测试
├── templates/             # 命令模板
├── examples/              # 示例项目
└── docs/                  # 文档
```

## 代码规范

### TypeScript 风格

- 使用 TypeScript strict 模式
- 明确的类型注解
- 避免 `any` 类型
- 使用 ES2022 模块

**示例**:
```typescript
// ✅ 好的
function analyzeContent(markdown: string): ContentAnalysis {
  // ...
}

// ❌ 不好的
function analyzeContent(markdown: any) {
  // ...
}
```

### 命名规范

- **变量/函数**: camelCase
- **类**: PascalCase
- **常量**: UPPER_SNAKE_CASE
- **接口/类型**: PascalCase

```typescript
// ✅ 好的
const targetScore = 8
class EEATScorer { }
interface ContentAnalysis { }
const MAX_SCORE = 10

// ❌ 不好的
const TargetScore = 8
class eeat_scorer { }
```

### 文件组织

- 一个文件一个类/功能
- 导出在文件末尾
- 使用 `.js` 扩展名导入(ES 模块)

```typescript
// ✅ 好的
import { ContentAnalyzer } from './content-analyzer.js'

// ❌ 不好的
import { ContentAnalyzer } from './content-analyzer'
```

### 注释规范

```typescript
/**
 * 分析 Markdown 文章的内容
 *
 * @param markdown - Markdown 文本
 * @returns 内容分析结果
 */
function analyze(markdown: string): ContentAnalysis {
  // 解析 frontmatter
  const { data, content } = matter(markdown)

  // 返回分析结果
  return { /* ... */ }
}
```

## 提交指南

### 提交消息格式

使用 [Conventional Commits](https://www.conventionalcommits.org/) 规范:

```
<type>(<scope>): <subject>

<body>

<footer>
```

### Type 类型

- `feat`: 新功能
- `fix`: Bug 修复
- `docs`: 文档更新
- `test`: 测试相关
- `refactor`: 重构
- `style`: 代码格式
- `chore`: 构建/工具
- `perf`: 性能优化

### 示例

```bash
# 新功能
git commit -m "feat(scorer): add custom weight support"

# Bug 修复
git commit -m "fix(schema): correct author extraction from frontmatter"

# 文档
git commit -m "docs: add installation guide"

# 测试
git commit -m "test: add edge case tests for content analyzer"
```

### 提交最佳实践

1. **原子性提交**: 一个提交只做一件事
2. **清晰的消息**: 说明做了什么和为什么
3. **测试通过**: 提交前运行 `npm test`
4. **代码构建**: 提交前运行 `npm run build`

## 测试要求

### 测试覆盖

所有新功能和 Bug 修复都必须包含测试:

```typescript
describe('NewFeature', () => {
  it('should work correctly', () => {
    const result = newFeature('input')
    expect(result).toBe('expected')
  })

  it('should handle edge cases', () => {
    expect(newFeature('')).toBe('')
    expect(newFeature(null)).toThrow()
  })
})
```

### 运行测试

```bash
# 运行所有测试
npm test

# 运行特定文件
npm test -- content-analyzer.test.ts

# 运行特定测试
npm test -- -t "should detect Chinese"

# 生成覆盖率
npm run test:coverage
```

### 测试标准

- ✅ 单元测试覆盖所有公共方法
- ✅ 测试正常路径和边界情况
- ✅ 测试名称清晰描述行为
- ✅ 每个测试只验证一个行为
- ✅ 使用有意义的测试数据

## Pull Request 检查清单

提交 PR 前,确保:

- [ ] 代码遵循项目规范
- [ ] 添加了必要的测试
- [ ] 所有测试通过 (`npm test`)
- [ ] 代码成功构建 (`npm run build`)
- [ ] 更新了相关文档
- [ ] 提交消息符合规范
- [ ] PR 描述清晰说明了变更内容

### PR 模板

```markdown
## 变更类型
- [ ] Bug 修复
- [ ] 新功能
- [ ] 文档更新
- [ ] 代码重构
- [ ] 性能优化

## 变更说明
简要描述这个 PR 做了什么

## 相关 Issue
Closes #123

## 测试
如何测试这个变更?

## 截图(如适用)

## 检查清单
- [ ] 代码遵循项目规范
- [ ] 添加了测试
- [ ] 所有测试通过
- [ ] 更新了文档
```

## 代码审查

### 审查标准

1. **功能正确性**: 代码是否实现了预期功能?
2. **代码质量**: 是否清晰、可维护?
3. **测试完整性**: 是否有足够的测试覆盖?
4. **性能**: 是否有性能问题?
5. **安全性**: 是否有安全隐患?

### 审查时间

我们会尽快审查 PR,通常在 48 小时内。

## 发布流程

只有维护者可以发布新版本:

1. 更新版本号 (`package.json`)
2. 更新 CHANGELOG.md
3. 创建 Git 标签
4. 发布到 npm
5. 创建 GitHub Release

## 许可证

贡献的代码将使用 MIT 许可证。

## 获取帮助

- **文档**: [README.md](README.md)
- **Issues**: [GitHub Issues](https://github.com/wordflowlab/geoify/issues)
- **讨论**: [GitHub Discussions](https://github.com/wordflowlab/geoify/discussions)

---

**感谢你的贡献!** 🎉
